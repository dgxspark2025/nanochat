```
# uv run is equivalent to activating the virtual environment first, but more convenient for single commands.
uv run python -m myscripts.image --load-only
```

```
uv run python -m myscripts.mnist.image_by_patch
```

```
uv run python -m myscripts.tiny_imagenet.image_by_patch
```
